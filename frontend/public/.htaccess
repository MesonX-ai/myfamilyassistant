<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # SPA routing: rewrite non-existent files/dirs to index.html for myfamilyassistant.ai
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ index.html [L]
</IfModule>



