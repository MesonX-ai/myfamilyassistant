<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # Rebase to the myfamilyassistant.ai subdirectory
  RewriteBase /myfamilyassistant.ai/
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ index.html [L]
</IfModule>
